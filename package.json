{
    "name": "taskflow-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "test": "vitest run",
        "test:watch": "vitest",
        "backend:start": "npm start --prefix src/backend",
        "frontend:start": "npm run dev --prefix src/frontend"
    },
    "devDependencies": {
        "vitest": "^1.0.0",
        "jsdom": "^22.1.0"
    }
}